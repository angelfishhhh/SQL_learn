# SQL 错题本

## 一、错题总表

| 编号 | 日期 | 题目来源 | 题目名称 | 一级类型 | 二级类型 | 错误原因 | 是否二刷 | 是否三刷 |
|---|---|---|---|---|---|---|---|---|
| 584 | 2026-5-27 | LeetCode | 寻找用户推荐人 | select | NULL处理 | IS NULL | 否 | 否 |
| 1661 | 2026-5-28 | LeetCode | 每台机器的进程平均运行时间 |  |  | 真正除的是进程数量 |  |  |
| 1280 | 2026-5-28 | LeetCode | 学生们参加各科测试的次数 |  |  |  |  |  |
| 570 | 2026-5-29 | LeetCode | 至少有5名直接下属的的经理 |  |  |  |  |  |
| 180 | 2026-6-01 | LeetCode | 寻找连续数字 |  |  |  |  |  |
| 1907 | 2026-6-02 | LeetCode | 按分类统计薪水 |  |  |  |  |  |



---

## 二、单题错题记录模板

# 题目编号：LeetCode 584

## 1. 题目信息

题目来源：LeetCode SQL 50  
题目名称：Find Customer Referee / 寻找用户推荐人  
完成日期：2026-5-27  
是否通过：否  
是否看答案：是  

---

## 2. 题目类型

一级类型：基础查询 / NULL 处理  
二级类型：where 条件过滤 / 空值判断  

本题类型：判断 referee_id 不等于 2 或 referee_id 为空的客户。

---

## 3. 表结构与粒度分析

表名：Customer

字段：

- id：客户 ID
- name：客户姓名
- referee_id：推荐该客户的客户 ID，可能为空

表粒度：

Customer 表一行代表一个客户。

是否涉及 join：

否。

join 后行数是否可能变多：

否。

---

## 4. 目标输出分析

最终结果一行代表什么：

一行代表一个没有被 id = 2 的客户推荐的客户姓名。

需要输出哪些字段：

name。

是否需要去重：

不需要。题目没有要求去重。

是否需要分组：

不需要。

是否需要排序 / 排名：

不需要。

---

## 5. 我的错误 SQL

```sql
SELECT name
FROM Customer
WHERE referee_id != 2
OR referee_id = NULL
;
```


# 题目编号：LeetCode 1661

## 1. 题目信息

题目来源：LeetCode SQL 50  
题目名称：Average Time of Process per Machine / 每台机器的进程平均运行时间  
完成日期：2026-5-28  
是否通过：否  
是否看答案：未说明  

---

## 2. 题目类型

一级类型：聚合查询 / 自连接  
二级类型：GROUP BY 分组 / AVG 平均值 / ROUND 保留小数  

本题类型：根据同一台机器、同一个进程的 `start` 和 `end` 两条记录，计算每个进程耗时，再按机器求平均耗时。

---

## 3. 表结构与粒度分析

表名：Activity

字段：

- machine_id：机器 ID
- process_id：进程 ID
- activity_type：活动类型，只可能是 `'start'` 或 `'end'`
- timestamp：当前时间，单位为秒

表粒度：

Activity 表一行代表某台机器的某个进程的一次活动记录。

一个完整进程会有两行：

```text
一行 start
一行 end
```

是否涉及 join：

是，可以使用自连接。

join 后行数是否可能变多：

会。

如果只按：

```sql
a1.machine_id = a2.machine_id
AND a1.process_id = a2.process_id
```

连接，那么同一个进程的两行记录会互相匹配，形成 4 行：

```text
start - start
start - end
end   - start
end   - end
```

所以必须限制：

```sql
a1.activity_type = 'start'
AND a2.activity_type = 'end'
```

这样每个进程只保留一行有效记录。

---

## 4. 目标输出分析

最终结果一行代表什么：

一行代表一台机器的平均进程处理时间。

需要输出哪些字段：

- machine_id
- processing_time

是否需要去重：

不需要。

是否需要分组：

需要。

按 `machine_id` 分组。

是否需要排序 / 排名：

不需要。题目要求结果可以按任意顺序返回。

---

## 5. 我的错误 SQL

```sql
SELECT a1.machine_id, ROUND(SUM(ABS(a1.timestamp - a2.timestamp)) / 4, 3) AS processing_time
FROM Activity AS a1
JOIN Activity AS a2
ON a1.machine_id = a2.machine_id
    AND a1.process_id = a2.process_id
GROUP BY machine_id
;
```

错误点 1：自连接后没有区分 `start` 和 `end`

你只按照 `machine_id` 和 `process_id` 连接，会导致每个进程产生 4 行：

```text
start - start
start - end
end   - start
end   - end
```

其中只有 `start - end` 才是我们真正需要的。

错误点 2：`ABS(a1.timestamp - a2.timestamp)` 是在补救错误连接

因为没有限制谁是开始、谁是结束，所以用了 `ABS()` 取绝对值。

但本题的正确逻辑应该是：

```sql
end.timestamp - start.timestamp
```

而不是让所有组合都参与计算。

错误点 3：`/ 4` 是硬编码，不可靠

在样例中，每个进程错误连接后产生 4 行，所以你用 `/ 4` 可能看起来合理。

但正确逻辑不是除以 4，而是：

```sql
AVG(每个进程的结束时间 - 开始时间)
```

也就是每个进程先算一次耗时，再对机器求平均。

---

## 6. 正确 SQL

```sql
SELECT 
    a1.machine_id,
    ROUND(AVG(a2.timestamp - a1.timestamp), 3) AS processing_time
FROM Activity AS a1
JOIN Activity AS a2
ON a1.machine_id = a2.machine_id
    AND a1.process_id = a2.process_id
    AND a1.activity_type = 'start'
    AND a2.activity_type = 'end'
GROUP BY a1.machine_id;
```

---

## 7. 解题思路

先把一条 `start` 记录和它对应的 `end` 记录连接到同一行。

连接条件是：

```sql
a1.machine_id = a2.machine_id
AND a1.process_id = a2.process_id
```

但这还不够，还要指定：

```sql
a1.activity_type = 'start'
AND a2.activity_type = 'end'
```

这样一行就代表一个完整进程。

然后用：

```sql
a2.timestamp - a1.timestamp
```

计算这个进程的处理时间。

最后按机器分组：

```sql
GROUP BY a1.machine_id
```

再求平均值：

```sql
AVG(a2.timestamp - a1.timestamp)
```

并保留 3 位小数：

```sql
ROUND(..., 3)
```

---

## 8. 本题易错点总结

这题的关键不是先 `SUM`，而是先识别一对 `start` 和 `end`。

看到一张表里同一个实体有两种状态：

```text
start
end
```

通常要想到两种做法：

第一种是自连接，把 `start` 和 `end` 拼成一行。

第二种是条件聚合，用 `CASE WHEN` 或 `IF` 把 `start` 当成负数，`end` 当成正数。

本题建议优先掌握自连接写法，因为它更直观：

```text
start 行 + end 行 → 一个完整进程 → 求耗时 → 按机器求平均
```

---

## 9. 另一种写法：条件聚合

```sql
SELECT
    machine_id,
    ROUND(
        AVG(
            CASE 
                WHEN activity_type = 'start' THEN -timestamp
                ELSE timestamp
            END
        ) * 2,
        3
    ) AS processing_time
FROM Activity
GROUP BY machine_id;
```

这个写法的含义是：

对于每台机器，每个进程有两行：

```text
start 记为 -timestamp
end   记为 +timestamp
```

所以同一个进程两行相加后就是：

```text
end timestamp - start timestamp
```

因为 `AVG()` 是对所有 `start` 和 `end` 行求平均，每个进程有 2 行，所以最后需要乘以 2。

---

## 10. 记忆模板

遇到同一张表中有开始和结束两种记录时，可以优先考虑自连接：

```sql
SELECT
    start_table.分组字段,
    ROUND(AVG(end_table.时间字段 - start_table.时间字段), 3) AS 结果字段
FROM 表名 AS start_table
JOIN 表名 AS end_table
ON start_table.匹配字段1 = end_table.匹配字段1
    AND start_table.匹配字段2 = end_table.匹配字段2
    AND start_table.状态字段 = 'start'
    AND end_table.状态字段 = 'end'
GROUP BY start_table.分组字段;
```

# 题目编号：LeetCode 1280

## 1. 题目信息

题目来源：LeetCode SQL 50  
题目名称：Students and Examinations / 学生们参加各科测试的次数  
完成日期：2026-5-28  
是否通过：否  
是否看答案：未说明  

---

## 2. 题目类型

一级类型：多表查询 / 聚合查询  
二级类型：CROSS JOIN / LEFT JOIN / GROUP BY / COUNT  

本题类型：统计每个学生参加每门考试的次数，包括参加次数为 0 的情况。

---

## 3. 表结构与粒度分析

表名：Students

字段：

- student_id：学生 ID
- student_name：学生姓名

表粒度：

Students 表一行代表一个学生。

---

表名：Subjects

字段：

- subject_name：科目名称

表粒度：

Subjects 表一行代表一个科目。

---

表名：Examinations

字段：

- student_id：学生 ID
- subject_name：科目名称

表粒度：

Examinations 表一行代表某个学生参加了一次某个科目的考试。

注意：

Examinations 表没有主键，可能存在重复行。

如果同一个学生多次参加同一门科目的考试，那么 Examinations 表中会有多行相同的：

```text
student_id + subject_name
```

这些重复行不是错误数据，而是代表参加了多次考试，需要被统计进去。

---

是否涉及 join：

是。

涉及三张表：

```text
Students
Subjects
Examinations
```

join 后行数是否可能变多：

会。

首先需要用 `CROSS JOIN` 生成所有学生和所有科目的组合：

```text
每个学生 × 每个科目
```

然后再用 `LEFT JOIN` 去匹配实际考试记录。

---

## 4. 目标输出分析

最终结果一行代表什么：

一行代表某个学生在某一门科目上的考试参加次数。

需要输出哪些字段：

- student_id
- student_name
- subject_name
- attended_exams

是否需要去重：

不需要。

是否需要分组：

需要。

按以下字段分组：

```sql
s.student_id,
s.student_name,
sub.subject_name
```

是否需要排序 / 排名：

需要排序。

按照：

```sql
student_id,
subject_name
```

升序排序。

---

## 5. 我的错误 SQL

```sql
SELECT e.student_id, s.student_name, e.subject_name, COUNT(e.subject_name) AS attended_exams
FROM Examinations AS e
JOIN Students AS s
ON s.student_id = e.student_id
ORDER BY student_id
;
```

错误点 1：从 `Examinations` 表出发

你的 SQL 是从 `Examinations` 表开始查：

```sql
FROM Examinations AS e
```

但 `Examinations` 表只记录了已经参加过考试的数据。

如果某个学生没有参加某门考试，那么这张表里根本没有对应记录。

而题目要求：

```text
即使参加次数为 0，也要显示出来
```

所以不能只从 `Examinations` 表出发。

---

错误点 2：没有使用 `Subjects` 表

题目要求统计每个学生参加每门科目的次数。

这里的“每门科目”来自 `Subjects` 表。

你的 SQL 只用了：

```text
Examinations
Students
```

没有使用：

```text
Subjects
```

因此无法生成所有学生和所有科目的完整组合。

---

错误点 3：使用了 `JOIN`，会过滤掉没有考试记录的情况

你的 SQL 使用的是：

```sql
JOIN Students AS s
ON s.student_id = e.student_id
```

这里的 `JOIN` 等价于 `INNER JOIN`。

`INNER JOIN` 只会保留匹配成功的记录。

如果某个学生没有参加某门考试，那么这组记录不会出现在 `Examinations` 表里，也就不会出现在最终结果中。

但本题要求参加次数为 0 的情况也要显示，所以后面连接考试记录时应该使用：

```sql
LEFT JOIN
```

---

错误点 4：缺少 `GROUP BY`

你使用了聚合函数：

```sql
COUNT(e.subject_name)
```

同时又查询了普通字段：

```sql
e.student_id,
s.student_name,
e.subject_name
```

这种情况下应该使用 `GROUP BY`。

否则 SQL 逻辑不完整。

正确分组应该是：

```sql
GROUP BY
    s.student_id,
    s.student_name,
    sub.subject_name
```

---

错误点 5：排序条件不完整

题目要求按照：

```sql
student_id,
subject_name
```

排序。

你的 SQL 只写了：

```sql
ORDER BY student_id
```

缺少：

```sql
subject_name
```

---

## 6. 正确 SQL

```sql
SELECT
    s.student_id,
    s.student_name,
    sub.subject_name,
    COUNT(e.subject_name) AS attended_exams
FROM Students AS s
CROSS JOIN Subjects AS sub
LEFT JOIN Examinations AS e
ON s.student_id = e.student_id
    AND sub.subject_name = e.subject_name
GROUP BY
    s.student_id,
    s.student_name,
    sub.subject_name
ORDER BY
    s.student_id,
    sub.subject_name;
```

---

## 7. 解题思路

本题的关键是：

不能只统计 `Examinations` 表。

因为 `Examinations` 表只记录已经参加过考试的记录。

如果某个学生没有参加某门考试，那么 `Examinations` 表中没有对应行。

但题目要求：

```text
参加次数为 0 的情况也要显示
```

所以第一步应该先生成完整的结果框架：

```text
所有学生 × 所有科目
```

使用：

```sql
FROM Students AS s
CROSS JOIN Subjects AS sub
```

这样可以得到每个学生和每个科目的组合。

然后再用 `LEFT JOIN` 去连接实际考试记录：

```sql
LEFT JOIN Examinations AS e
ON s.student_id = e.student_id
    AND sub.subject_name = e.subject_name
```

这里必须同时匹配两个条件：

```text
学生相同
科目相同
```

最后用：

```sql
COUNT(e.subject_name)
```

统计考试次数。

如果没有匹配到考试记录，`e.subject_name` 是 `NULL`。

而：

```sql
COUNT(字段名)
```

不会统计 `NULL`，所以结果会是 0。

---

## 8. 为什么使用 CROSS JOIN

`CROSS JOIN` 用来生成笛卡尔积。

在本题中，它的作用是生成所有可能的：

```text
学生 - 科目
```

组合。

例如：

```text
3 个学生
3 门科目
```

会生成：

```text
3 × 3 = 9
```

行。

这样才能保证没有参加考试的学生-科目组合也会出现在最终结果中。

---

## 9. 为什么使用 LEFT JOIN

生成完整的学生和科目组合后，需要去 `Examinations` 表中匹配真实考试记录。

这里不能使用 `INNER JOIN`。

因为 `INNER JOIN` 只会保留匹配成功的记录。

如果某个学生没有参加某门考试，那么这组记录会被过滤掉。

本题要求保留参加次数为 0 的情况，所以必须使用：

```sql
LEFT JOIN
```

---

## 10. COUNT 的选择

本题建议使用：

```sql
COUNT(e.subject_name)
```

或者：

```sql
COUNT(e.student_id)
```

不建议使用：

```sql
COUNT(*)
```

原因是：

`LEFT JOIN` 后，即使没有匹配到考试记录，左表中的学生-科目组合仍然会保留一行。

如果使用：

```sql
COUNT(*)
```

那么没有参加考试的情况也会被统计为 1。

而使用：

```sql
COUNT(e.subject_name)
```

时，如果没有匹配到考试记录，`e.subject_name` 是 `NULL`。

`COUNT(字段名)` 不统计 `NULL`，所以结果会是 0。

---

## 11. 本题易错点总结

易错点 1：从 `Examinations` 表出发

错误原因：

`Examinations` 只包含参加过考试的数据，不能得到参加次数为 0 的组合。

---

易错点 2：没有使用 `Subjects` 表

错误原因：

题目要求统计每个学生在每门科目上的考试次数。

如果不使用 `Subjects` 表，就无法得到完整的科目列表。

---

易错点 3：没有使用 `CROSS JOIN`

错误原因：

本题需要先生成：

```text
每个学生 × 每个科目
```

这个完整结果框架。

---

易错点 4：使用 `INNER JOIN`

错误原因：

`INNER JOIN` 会过滤掉没有考试记录的情况。

本题要求保留 0 次考试的情况，所以应该使用 `LEFT JOIN`。

---

易错点 5：缺少 `GROUP BY`

错误原因：

使用 `COUNT()` 统计时，需要按学生和科目分组。

---

易错点 6：使用 `COUNT(*)`

错误原因：

`COUNT(*)` 会把 `LEFT JOIN` 后保留下来的空匹配行也统计进去，导致 0 次考试被错误统计为 1。

---

## 12. 记忆模板

遇到题目要求：

```text
每个 A 对每个 B 的统计结果
```

并且包括次数为 0 的情况时，可以考虑：

```sql
SELECT
    A.字段,
    B.字段,
    COUNT(C.字段) AS 统计结果
FROM A
CROSS JOIN B
LEFT JOIN C
ON A.匹配字段 = C.匹配字段
    AND B.匹配字段 = C.匹配字段
GROUP BY
    A.字段,
    B.字段
ORDER BY
    A.字段,
    B.字段;
```

本题对应为：

```text
每个学生 × 每个科目
```

然后统计对应的考试记录数量。

---

## 13. 核心记忆

本题的核心不是直接从 `Examinations` 表统计。

而是三步：

```text
1. Students CROSS JOIN Subjects
2. LEFT JOIN Examinations
3. COUNT(e.subject_name)
```

对应理解为：

```text
先造出完整表
再匹配已有考试记录
最后统计次数
```

# 题目编号：LeetCode 570

## 1. 题目信息

题目来源：LeetCode SQL 50  
题目名称：Managers with at Least 5 Direct Reports / 至少有 5 名直接下属的经理  
完成日期：2026-5-29  
是否通过：否  
是否看答案：未说明  

---

## 2. 题目类型

一级类型：分组统计 / 子查询 / 自连接  
二级类型：GROUP BY / HAVING / IN / COUNT  

本题类型：统计每个经理的直接下属人数，找出直接下属人数大于等于 5 的经理姓名。

---

## 3. 表结构与粒度分析

表名：Employee

字段：

- id：员工 ID
- name：员工姓名
- department：员工所在部门
- managerId：该员工的经理 ID，可能为空

表粒度：

Employee 表一行代表一个员工。

注意：

`managerId` 表示当前员工的直属经理是谁。

例如：

```text
员工 A 的 managerId = 101
```

表示：

```text
员工 A 的直属经理是 id = 101 的员工
```

如果 `managerId` 为 `NULL`，表示这个员工没有经理。

---

是否涉及 join：

可以涉及自连接，也可以使用子查询。

join 后行数是否可能变多：

会。

如果使用自连接：

```sql
e1.id = e2.managerId
```

含义是：

```text
e1 是经理
e2 是 e1 的直接下属
```

一个经理如果有多个直接下属，自连接后就会对应多行。

---

## 4. 目标输出分析

最终结果一行代表什么：

一行代表一个至少有 5 名直接下属的经理。

需要输出哪些字段：

- name

是否需要去重：

不需要。

因为 `Employee.id` 是主键，每个经理只对应一行员工记录。

是否需要分组：

需要。

需要按经理分组，统计每个经理有多少个直接下属。

是否需要排序 / 排名：

不需要。题目要求结果可以按任意顺序返回。

---

## 5. 我的错误 SQL

```sql
SELECT name
FROM Employee
WHERE
    id =
    (
        SELECT managerId
        FROM Employee
        GROUP BY managerId
        HAVING COUNT(*) >= 5
    )
;
```

错误点 1：`=` 只能匹配单个值

你的子查询是：

```sql
SELECT managerId
FROM Employee
GROUP BY managerId
HAVING COUNT(*) >= 5
```

它的结果可能返回多个 `managerId`。

例如：

```text
101
102
103
```

但外层使用的是：

```sql
WHERE id = (...)
```

`=` 后面的子查询应该只返回一个值。

如果子查询返回多个值，就会报错。

正确写法应该使用：

```sql
WHERE id IN (...)
```

因为我们要判断：

```text
员工 id 是否在这些 managerId 里面
```

---

错误点 2：没有排除 `managerId IS NULL`

你的子查询中：

```sql
GROUP BY managerId
```

会把 `NULL` 也分成一组。

如果有很多员工都没有经理，那么：

```text
managerId = NULL
```

这一组也可能满足：

```sql
HAVING COUNT(*) >= 5
```

虽然最终 `id IN (NULL)` 不会匹配出员工，但逻辑上最好明确排除：

```sql
WHERE managerId IS NOT NULL
```

这样子查询只统计真正的经理 ID。

---

错误点 3：`COUNT(*)` 本身不是最大问题，但要理解它的含义

在子查询中：

```sql
COUNT(*)
```

统计的是每个 `managerId` 出现了多少次。

也就是：

```text
有多少个员工的 managerId 等于这个经理 id
```

这正好表示这个经理有多少个直接下属。

所以在排除 `managerId IS NOT NULL` 后，`COUNT(*)` 可以使用。

也可以写成：

```sql
COUNT(managerId)
```

---

## 6. 正确 SQL：子查询写法

```sql
SELECT name
FROM Employee
WHERE id IN (
    SELECT managerId
    FROM Employee
    WHERE managerId IS NOT NULL
    GROUP BY managerId
    HAVING COUNT(*) >= 5
);
```

---

## 7. 解题思路：子查询写法

第一步，先统计每个经理有多少个直接下属。

```sql
SELECT managerId
FROM Employee
WHERE managerId IS NOT NULL
GROUP BY managerId
HAVING COUNT(*) >= 5
```

这里的逻辑是：

```text
按照 managerId 分组
每一组代表一个经理
COUNT(*) 统计这个经理有几个直接下属
HAVING COUNT(*) >= 5 筛选出至少有 5 个直接下属的经理
```

第二步，回到 `Employee` 表中找这些经理的姓名。

```sql
SELECT name
FROM Employee
WHERE id IN (...)
```

因为：

```text
Employee.id = 经理自己的员工 ID
Employee.managerId = 下属记录中的经理 ID
```

所以要用：

```sql
id IN (符合条件的 managerId)
```

---

## 8. 正确 SQL：自连接写法

```sql
SELECT
    e1.name
FROM Employee AS e1
JOIN Employee AS e2
ON e1.id = e2.managerId
GROUP BY
    e1.id,
    e1.name
HAVING COUNT(e2.id) >= 5;
```

---

## 9. 解题思路：自连接写法

把 `Employee` 表看成两张表：

```text
e1：经理表
e2：员工表
```

连接条件是：

```sql
e1.id = e2.managerId
```

含义是：

```text
e2 这个员工的 managerId 等于 e1 的 id
```

也就是说：

```text
e1 是 e2 的经理
```

连接后，一个经理有几个直接下属，就会出现几行。

然后按照经理分组：

```sql
GROUP BY e1.id, e1.name
```

再统计下属人数：

```sql
COUNT(e2.id)
```

最后筛选：

```sql
HAVING COUNT(e2.id) >= 5
```

---

## 10. 本题易错点总结

易错点 1：把 `id` 和 `managerId` 的关系看反

`id` 是员工自己的 ID。

`managerId` 是这个员工的经理 ID。

所以：

```text
某个人的 id 出现在别人的 managerId 中
```

才说明这个人是经理。

---

易错点 2：使用 `WHERE id = (子查询)`

如果子查询可能返回多个值，不能使用：

```sql
=
```

应该使用：

```sql
IN
```

---

易错点 3：忘记使用 `HAVING`

`WHERE` 是分组前过滤行。

`HAVING` 是分组后过滤组。

本题要筛选：

```text
直接下属人数 >= 5
```

这是聚合统计后的结果，所以必须使用：

```sql
HAVING COUNT(*) >= 5
```

---

易错点 4：没有排除 `managerId IS NULL`

`managerId` 为 `NULL` 表示没有经理。

它不是真正的经理 ID。

所以在子查询写法中，最好加上：

```sql
WHERE managerId IS NOT NULL
```

---

## 11. 记忆模板

遇到题目要求：

```text
找出拥有至少 N 个关联对象的人
```

可以先按关联字段分组统计：

```sql
SELECT 主表字段
FROM 主表
WHERE 主键 IN (
    SELECT 关联字段
    FROM 主表
    WHERE 关联字段 IS NOT NULL
    GROUP BY 关联字段
    HAVING COUNT(*) >= N
);
```

本题对应为：

```text
找出至少有 5 个直接下属的经理
```

所以是：

```sql
SELECT name
FROM Employee
WHERE id IN (
    SELECT managerId
    FROM Employee
    WHERE managerId IS NOT NULL
    GROUP BY managerId
    HAVING COUNT(*) >= 5
);
```

---

## 12. 核心记忆

本题的核心是：

```text
managerId 出现了几次，就代表这个经理有几个直接下属
```

所以三步：

```text
1. 按 managerId 分组
2. HAVING COUNT(*) >= 5
3. 用 id IN (...) 找经理姓名
```

你的思路方向是对的，主要把：

```sql
id = (...)
```

改成：

```sql
id IN (...)
```

并补上：

```sql
WHERE managerId IS NOT NULL
```

# 题目编号：LeetCode 180

## 1. 题目信息

题目来源：LeetCode SQL 50  
题目名称：Consecutive Numbers / 连续出现的数字  
完成日期：2026-5-29  
是否通过：否  
是否看答案：未说明  

---

## 2. 题目类型

一级类型：自连接 / 连续记录判断  
二级类型：JOIN / DISTINCT / 连续 id 判断  

本题类型：找出至少连续出现 3 次的数字。

---

## 3. 表结构与粒度分析

表名：Logs

字段：

- id：日志 ID，是自增主键
- num：当前日志记录中的数字

表粒度：

Logs 表一行代表一条日志记录。

注意：

本题中的连续不是指数值连续，而是指行的位置连续。

也就是说，需要判断：

```text
id 连续
num 相同
```

例如：

```text
id = 1, num = 1
id = 2, num = 1
id = 3, num = 1
```

这表示数字 `1` 连续出现了 3 次。

---

是否涉及 join：

可以涉及自连接。

join 后行数是否可能变多：

会。

因为同一张 `Logs` 表需要连接 3 次：

```text
l1：第一行
l2：第二行
l3：第三行
```

通过 id 的关系判断它们是否连续。

---

## 4. 目标输出分析

最终结果一行代表什么：

一行代表一个至少连续出现 3 次的数字。

需要输出哪些字段：

- ConsecutiveNums

是否需要去重：

需要。

如果某个数字连续出现超过 3 次，例如连续出现 4 次：

```text
1, 1, 1, 1
```

那么会形成两组三连：

```text
第 1、2、3 行
第 2、3、4 行
```

如果不去重，结果中会出现重复数字。

所以需要使用：

```sql
DISTINCT
```

是否需要分组：

不需要。

是否需要排序 / 排名：

不需要。题目要求结果可以按任意顺序返回。

---

## 5. 我的错误 SQL

```sql
SELECT l1.num AS ConsecutiveNums 
FROM Logs AS l1
JOIN Logs AS l2
ON l1.id = (l2.id - 1)
JOIN Logs AS l3
ON l1.id = (l3.id - 2)
WHERE l1.num = l2.num = l3.num
;
```

错误点 1：SQL 中不能这样连续比较

你写的是：

```sql
WHERE l1.num = l2.num = l3.num
```

这不是 SQL 中判断三个值相等的正确写法。

SQL 不会把它理解成：

```text
l1.num 等于 l2.num，并且 l2.num 等于 l3.num
```

正确写法应该是：

```sql
WHERE l1.num = l2.num
  AND l2.num = l3.num
```

也可以写成：

```sql
WHERE l1.num = l2.num
  AND l1.num = l3.num
```

---

错误点 2：缺少 `DISTINCT`

如果某个数字连续出现 4 次：

```text
id:  1  2  3  4
num: 1  1  1  1
```

那么你的自连接会找到两组满足条件的三连：

```text
1, 2, 3
2, 3, 4
```

如果直接：

```sql
SELECT l1.num
```

结果会返回两个 `1`。

但题目只需要返回数字本身，所以应该使用：

```sql
SELECT DISTINCT l1.num AS ConsecutiveNums
```

---

错误点 3：连接条件方向可以，但不够直观

你写的连接条件是：

```sql
ON l1.id = (l2.id - 1)
```

等价于：

```sql
ON l2.id = l1.id + 1
```

也就是说：

```text
l2 是 l1 的下一行
```

这本身没有问题。

你写的：

```sql
ON l1.id = (l3.id - 2)
```

等价于：

```sql
ON l3.id = l1.id + 2
```

也就是说：

```text
l3 是 l1 的下下行
```

这也没有问题。

只是为了阅读更清楚，通常会写成：

```sql
l2.id = l1.id + 1
l3.id = l1.id + 2
```

或者：

```sql
l1.id = l2.id - 1
l2.id = l3.id - 1
```

---

## 6. 正确 SQL：自连接写法

```sql
SELECT DISTINCT
    l1.num AS ConsecutiveNums
FROM Logs AS l1
JOIN Logs AS l2
ON l2.id = l1.id + 1
JOIN Logs AS l3
ON l3.id = l1.id + 2
WHERE l1.num = l2.num
  AND l2.num = l3.num;
```

---

## 7. 解题思路：自连接写法

把同一张 `Logs` 表看成三张表：

```text
l1：当前行
l2：下一行
l3：下下行
```

先通过 id 判断三行是否连续：

```sql
l2.id = l1.id + 1
l3.id = l1.id + 2
```

然后判断三行的数字是否相同：

```sql
l1.num = l2.num
AND l2.num = l3.num
```

如果这两个条件都满足，说明：

```text
同一个数字至少连续出现了 3 次
```

最后用：

```sql
DISTINCT
```

去掉重复结果。

---

## 8. 为什么需要 DISTINCT

假设数据是：

```text
id:  1  2  3  4
num: 7  7  7  7
```

这时数字 `7` 连续出现了 4 次。

自连接会找到：

```text
id 1, 2, 3
id 2, 3, 4
```

这两组都满足连续 3 次。

如果不用 `DISTINCT`，结果会是：

```text
7
7
```

但题目只要求返回出现连续 3 次的数字，所以最终应该是：

```text
7
```

因此需要：

```sql
SELECT DISTINCT
```

---

## 9. 本题易错点总结

易错点 1：误以为 `l1.num = l2.num = l3.num` 可以判断三个值相等

错误原因：

SQL 中不能用这种方式连续比较多个字段。

正确写法是：

```sql
l1.num = l2.num
AND l2.num = l3.num
```

---

易错点 2：忘记判断 id 连续

本题不是找出现次数大于等于 3 的数字。

错误理解是：

```text
某个 num 总共出现 3 次
```

正确理解是：

```text
某个 num 连续出现 3 次
```

所以必须判断 id 是否连续。

---

易错点 3：忘记使用 DISTINCT

如果同一个数字连续出现超过 3 次，会产生多个满足条件的三连组合。

所以要去重。

---

易错点 4：把连续数字理解成数字大小连续

本题的连续指的是行连续，不是数字本身连续。

例如：

```text
1, 2, 3
```

这不是本题要找的连续。

本题要找的是：

```text
1, 1, 1
```

或者：

```text
2, 2, 2
```

---

## 10. 另一种写法：窗口函数

```sql
SELECT DISTINCT
    num AS ConsecutiveNums
FROM (
    SELECT
        id,
        num,
        LAG(num, 1) OVER (ORDER BY id) AS prev_num_1,
        LAG(num, 2) OVER (ORDER BY id) AS prev_num_2
    FROM Logs
) AS t
WHERE num = prev_num_1
  AND num = prev_num_2;
```

这个写法的含义是：

对于每一行，取它前面的两行：

```text
当前行 num
上一行 num
上上一行 num
```

如果三者相等，就说明当前数字连续出现了 3 次。

---

## 11. 记忆模板

遇到题目要求判断连续 3 行时，可以考虑三表自连接：

```sql
SELECT DISTINCT
    t1.目标字段 AS 结果字段
FROM 表名 AS t1
JOIN 表名 AS t2
ON t2.id = t1.id + 1
JOIN 表名 AS t3
ON t3.id = t1.id + 2
WHERE t1.目标字段 = t2.目标字段
  AND t2.目标字段 = t3.目标字段;
```

本题对应为：

```sql
SELECT DISTINCT
    l1.num AS ConsecutiveNums
FROM Logs AS l1
JOIN Logs AS l2
ON l2.id = l1.id + 1
JOIN Logs AS l3
ON l3.id = l1.id + 2
WHERE l1.num = l2.num
  AND l2.num = l3.num;
```

---

## 12. 核心记忆

本题核心是两件事：

```text
1. id 连续：l2.id = l1.id + 1，l3.id = l1.id + 2
2. num 相同：l1.num = l2.num AND l2.num = l3.num
```

最后不要忘记：

```sql
DISTINCT
```

你的思路方向是对的，已经想到了三表自连接。

主要需要改三点：

```text
1. 连续比较改成 AND
2. SELECT 加 DISTINCT
3. 连接条件可以写得更直观
```
# 题目编号：LeetCode 1907

## 1. 题目信息

题目来源：LeetCode SQL 50  
题目名称：Count Salary Categories / 按分类统计薪水  
完成日期：2026-5-29  
是否通过：否  
是否看答案：未说明  

---

## 2. 题目类型

一级类型：条件分类 / 聚合统计  
二级类型：CASE WHEN / COUNT / UNION ALL / 补全 0 值类别  

本题类型：按照收入区间统计账户数量，并且必须返回所有薪资类别，即使某一类数量为 0。

---

## 3. 表结构与粒度分析

表名：Accounts

字段：

- account_id：账户 ID
- income：账户月收入

表粒度：

Accounts 表一行代表一个银行账户。

---

是否涉及 join：

不涉及。

join 后行数是否可能变多：

不涉及。

---

## 4. 目标输出分析

最终结果一行代表什么：

一行代表一个薪资类别及该类别下的账户数量。

需要输出哪些字段：

- category
- accounts_count

薪资类别分为三类：

```text
Low Salary：income < 20000
Average Salary：20000 <= income <= 50000
High Salary：income > 50000
```

是否需要去重：

不需要。

是否需要分组：

可以分组，但只用 `GROUP BY CASE WHEN` 会有一个问题：

```text
如果某个类别没有任何账户，就不会出现在结果中
```

本题要求三个类别都必须返回，所以更适合使用 `UNION ALL` 分别统计三类。

是否需要排序 / 排名：

不需要。题目要求结果可以按任意顺序返回。

---

## 5. 我的错误 SQL

```sql
SELECT
    CASE
        WHEN income < 20000 THEN 'Low Salary'
        WHEN income <= 50000 THEN 'Average Salary'
        ELSE 'High Salary'
    END AS category,
    COUNT(*) AS accounts_count
FROM Accounts
GROUP BY category;
```

错误点 1：只能统计实际存在的类别

你的 SQL 使用了：

```sql
GROUP BY category
```

这会按照账户实际所属的类别进行分组。

如果某个类别没有任何账户，比如没有 `Average Salary`，那么这一类不会出现在分组结果中。

但题目要求：

```text
即使某一类账户数量为 0，也要返回这一类
```

所以你的写法会漏掉数量为 0 的类别。

---

错误点 2：题目要求固定返回三行

本题最终结果应该固定包含三类：

```text
Low Salary
Average Salary
High Salary
```

而你的 SQL 返回的行数取决于 `Accounts` 表中实际存在多少种收入类别。

如果表中只有高收入账户，那么你的结果只会返回：

```text
High Salary
```

但题目需要返回：

```text
Low Salary      0
Average Salary  0
High Salary     实际数量
```

---

错误点 3：分类条件本身基本正确

你的分类条件是：

```sql
CASE
    WHEN income < 20000 THEN 'Low Salary'
    WHEN income <= 50000 THEN 'Average Salary'
    ELSE 'High Salary'
END
```

这部分逻辑本身没有问题。

因为第一层已经排除了：

```sql
income < 20000
```

所以第二层：

```sql
income <= 50000
```

实际表示：

```text
20000 <= income <= 50000
```

最后的：

```sql
ELSE 'High Salary'
```

表示：

```text
income > 50000
```

所以本题的主要错误不是分类边界，而是没有补全 0 值类别。

---

## 6. 正确 SQL：UNION ALL 写法

```sql
SELECT
    'Low Salary' AS category,
    COUNT(*) AS accounts_count
FROM Accounts
WHERE income < 20000

UNION ALL

SELECT
    'Average Salary' AS category,
    COUNT(*) AS accounts_count
FROM Accounts
WHERE income BETWEEN 20000 AND 50000

UNION ALL

SELECT
    'High Salary' AS category,
    COUNT(*) AS accounts_count
FROM Accounts
WHERE income > 50000;
```

---

## 7. 解题思路

本题不能只从已有数据中分组。

因为题目要求结果中必须包含三个固定类别：

```text
Low Salary
Average Salary
High Salary
```

所以更稳妥的做法是：

```text
分别统计三类
再用 UNION ALL 合并结果
```

第一类：

```sql
SELECT
    'Low Salary' AS category,
    COUNT(*) AS accounts_count
FROM Accounts
WHERE income < 20000
```

统计收入小于 20000 的账户数量。

第二类：

```sql
SELECT
    'Average Salary' AS category,
    COUNT(*) AS accounts_count
FROM Accounts
WHERE income BETWEEN 20000 AND 50000
```

统计收入在 20000 到 50000 之间的账户数量。

注意：

```sql
BETWEEN 20000 AND 50000
```

包含边界值 20000 和 50000。

第三类：

```sql
SELECT
    'High Salary' AS category,
    COUNT(*) AS accounts_count
FROM Accounts
WHERE income > 50000
```

统计收入大于 50000 的账户数量。

最后用：

```sql
UNION ALL
```

把三类结果合并成最终结果。

---

## 8. 为什么这里用 UNION ALL

`UNION ALL` 的作用是把多段查询结果上下拼接。

本题中，每一段查询都固定返回一类薪资：

```text
Low Salary
Average Salary
High Salary
```

即使某一类没有任何账户，`COUNT(*)` 也会返回 0。

例如：

```sql
SELECT COUNT(*)
FROM Accounts
WHERE income < 20000
```

如果没有符合条件的记录，结果仍然是一行：

```text
0
```

所以用 `UNION ALL` 可以保证最终一定返回三行。

---

## 9. 为什么不建议只用 GROUP BY CASE WHEN

你的写法本质是：

```sql
先给每个账户打标签
再按标签分组统计
```

这个思路在普通分类统计中是可以的。

但是本题额外要求：

```text
没有数据的类别也要显示为 0
```

而 `GROUP BY` 只能分出现过的类别。

如果某一类没有任何账户，就没有行可以参与分组，自然也不会出现在结果中。

所以本题不适合只用：

```sql
GROUP BY category
```

---

## 10. 另一种写法：SUM 条件判断

在 MySQL 中，也可以这样写：

```sql
SELECT
    'Low Salary' AS category,
    SUM(income < 20000) AS accounts_count
FROM Accounts

UNION ALL

SELECT
    'Average Salary' AS category,
    SUM(income BETWEEN 20000 AND 50000) AS accounts_count
FROM Accounts

UNION ALL

SELECT
    'High Salary' AS category,
    SUM(income > 50000) AS accounts_count
FROM Accounts;
```

这个写法利用了 MySQL 中条件表达式的特点：

```text
条件成立 = 1
条件不成立 = 0
```

所以：

```sql
SUM(income < 20000)
```

就等价于统计低收入账户数量。

但初学阶段更建议使用 `COUNT(*) + WHERE`，因为语义更直观。

---

## 11. 本题易错点总结

易错点 1：只用 `GROUP BY CASE WHEN`

错误原因：

只能返回实际出现过的类别，无法返回数量为 0 的类别。

---

易错点 2：忘记题目要求固定三类都返回

本题结果必须包含：

```text
Low Salary
Average Salary
High Salary
```

而不是只返回表中存在的类别。

---

易错点 3：边界条件写错

正确分类是：

```text
income < 20000
20000 <= income <= 50000
income > 50000
```

其中 `Average Salary` 包含 20000 和 50000。

---

易错点 4：误用 `UNION`

本题更建议使用：

```sql
UNION ALL
```

因为三类 category 本身不会重复，没有必要让 SQL 额外做去重。

---

## 12. 记忆模板

遇到题目要求：

```text
固定分类统计
并且没有数据的分类也要返回 0
```

可以考虑：

```sql
SELECT
    '类别1' AS category,
    COUNT(*) AS accounts_count
FROM 表名
WHERE 条件1

UNION ALL

SELECT
    '类别2' AS category,
    COUNT(*) AS accounts_count
FROM 表名
WHERE 条件2

UNION ALL

SELECT
    '类别3' AS category,
    COUNT(*) AS accounts_count
FROM 表名
WHERE 条件3;
```

本题对应为：

```sql
SELECT
    'Low Salary' AS category,
    COUNT(*) AS accounts_count
FROM Accounts
WHERE income < 20000

UNION ALL

SELECT
    'Average Salary' AS category,
    COUNT(*) AS accounts_count
FROM Accounts
WHERE income BETWEEN 20000 AND 50000

UNION ALL

SELECT
    'High Salary' AS category,
    COUNT(*) AS accounts_count
FROM Accounts
WHERE income > 50000;
```

---

## 13. 核心记忆

本题的核心不是会不会写 `CASE WHEN`。

而是要看清题目要求：

```text
三个类别都必须出现
没有账户的类别也要显示 0
```

所以：

```text
GROUP BY CASE WHEN
```

不够稳定。

更适合：

```text
UNION ALL 固定造出三行
每一行单独 COUNT
```

你的分类逻辑是对的，主要错在没有补全 0 次出现的类别。